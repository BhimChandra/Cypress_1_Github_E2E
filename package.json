{
  "name": "cypress_1_github_e2e",
  "version": "1.0.0",
  "description": "Adding and pushing code into Github",
  "main": "index.js",
  "scripts": {
    "test:test1_opensite": "npx cypress run --spec cypress/e2e/tests/test1_opensite.cy.js --browser chrome --headed",
    "test:test2_yatra": "npx cypress run --spec cypress/e2e/tests/test2_yatra.cy.js --browser chrome --headed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.12.0"
  }
}
